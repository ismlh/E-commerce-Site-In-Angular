import { BgBlackDirectiveDirective } from './bg-black-directive.directive';

describe('BgBlackDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new BgBlackDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
